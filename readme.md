# BiB

Binhost in a Box

Based on genTree

## Setup

To get started, a stage3 must be extracted to ~/.local/share/BiB/sysroots/default

> The name of the sysroot (here "default") can be changed by modifying the `name` field in the `config.toml` file.
